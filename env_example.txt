# Configuración de Base de Datos
MONGO_URI=mongodb+srv://usuario:password@cluster.mongodb.net/
DB_NAME=papeleria_db

# Configuración de Seguridad
CODE_SECRET_DATA=tu_clave_secreta_aqui

# Configuración SMTP para Envío de Emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu-email@gmail.com
SMTP_PASS=tu-password-de-aplicacion
SMTP_FROM=noreply@papeleria.com

# Configuración N8N para Envío de Códigos OTP
N8N_WEBHOOK_URL=http://localhost:5678/webhook/send-otp

# URL Base del Backend (opcional)
BASE_URL=https://back-papeleria-two.vercel.app

# Puerto del Servidor (opcional)
PORT=4000
